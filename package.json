{
  "name": "skill-router",
  "version": "0.2.0",
  "private": true,
  "description": "Local, dependency-free skill routing for OpenAI Codex.",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/search.test.js",
    "check": "node --check plugins/skill-router/hooks/user-prompt-submit.js && node --check plugins/skill-router/skills/skill-router/scripts/search-skills.js"
  },
  "engines": {
    "node": ">=18"
  }
}
